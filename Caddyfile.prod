:80 {
	# SSE: disable buffering so events are streamed immediately
	handle /api/sse {
		reverse_proxy frontdashboard-backend:8000 {
			flush_interval -1
		}
	}

	# API routes
	handle /api/* {
		# An oversized body is rejected here rather than after FastAPI has read and parsed it:
		# field-level max_length validators bound individual strings, nothing bounds the whole
		# body. 1MB is far above any real payload — the largest is a dashboard layout PUT, a few
		# KB. Not applied to /api/sse above, which is a long-lived response, not a body.
		request_body {
			max_size 1MB
		}
		reverse_proxy frontdashboard-backend:8000
	}

	# Static files with SPA fallback
	handle {
		root * /srv
		try_files {path} /index.html
		header {
			Content-Security-Policy "default-src 'self'; base-uri 'self'; frame-ancestors 'none'; object-src 'none'; form-action 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'"
			X-Permitted-Cross-Domain-Policies none
		}
		file_server
	}

	# Security headers
	header {
		Strict-Transport-Security "max-age=31536000"
		X-Frame-Options DENY
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
		Permissions-Policy "geolocation=(), microphone=(), camera=()"
		-Server
	}
}
