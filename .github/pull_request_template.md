<!-- Concise: ~800 characters for a small change, ~2000 for a large one. Bullets, one fact each.
     Write the change, not the investigation — how the problem was found belongs in the commit
     body. The `pr-checklist` skill owns the rest of the pre-merge sweep. -->

## Why

<!-- A short paragraph: the problem, and what should be true instead. -->

## What changed

<!-- One bullet per change — what it does now, not a walk through the diff. -->

-

## Test plan

<!-- `command — result, with numbers`. Say what is still unverified rather than omitting it.
     If this changes what a user sees, add screenshots of the rendered states, drag-dropped so
     GitHub stores them as attachments — never committed to the repo. -->

-

<!-- Add ONE more section above Test plan when it applies, and delete this comment:

     ## Compatibility — a migration (additive, so an image rollback is still safe; or destructive,
     so it is not), a regenerated API contract, anything an existing session would notice.
     ## Documentation — an ADR, FDR or CONTEXT.md change a reviewer should read first.
     ## Security — a change to auth, sharing, SSE audience or the retention sweeps.

     Then close with `Closes #123.` if it closes an issue. -->
